if err
    if err==="exist"
        span.label.label-danger.mt5 记录已存在
    if err === "wrong"
        span.label.label-danger.mt5 记录不匹配
    if err === "notexist"
        span.label.label-danger.mt5 记录不存在
    if err === "snotexist"
        span.label.label-danger.mt5 学校不存在
    if err === "hasname"
        span.label.label-danger.mt5 用户名重复
    if err==="wrongparams"
        span.label.label-danger.mt5 参数错误
    if err === "hasschool"
        span.label.label-danger.mt5 用户名下有学校，不能将'成为校长'选项置否